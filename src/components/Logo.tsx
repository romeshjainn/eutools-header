import { Box } from "@mui/material";

const Logo = () => {
  return (
    <Box className="logo_header" sx={{ position: "relative" }}>
      {/* Add logo here */}
    </Box>
  );
};

export default Logo;
